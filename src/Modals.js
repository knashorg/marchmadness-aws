import Modal from "react-bootstrap/Modal";
import Button from "react-bootstrap/Button";
import React from "react";

export function VerticallyCenteredModal7(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Boston University</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>27 - 10 - 0</h4>
        <p> 38 tournament appearances /
            Best Finish - Champions (5x) /
            Head Coach - Jay Pandolfo
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal2(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Canisius</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>20 - 18 - 3</h4>
        <p> 2 tournament appearances /
            Best Finish - DNP /
            Head Coach - Trevor Large
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal10(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Colgate</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>19 - 15 - 5</h4>
        <p> 6 tournament appearances /
            Best Finish - Runner-up /
            Head Coach - Don Vaughn
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal6(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Cornell</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>20 - 10 - 2</h4>
        <p> 23 tournament appearances /
            Best Finish - Champions (2x) /
            Head Coach - Mike Schafer
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal5(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Denver</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>30 - 9 - 0</h4>
        <p> 32 tournament appearances /
            Best Finish - Champions (9x) /
            Head Coach - David Carle
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal15(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Harvard</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>24 - 7 - 2</h4>
        <p> 27 tournament appearances /
            Best Finish - Champions /
            Head Coach - Ted Donato
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal14(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Merrimack</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>23 - 13 - 1</h4>
        <p> A3 tournament appearances /
            Best Finish - DNP /
            Head Coach - Scott Borek
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal9(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Michigan</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>24 - 11 - 3</h4>
        <p> 40 tournament appearances /
            Best Finish - Champions (9x) /
            Head Coach - Brandon Naurato (interim)
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal12(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Michigan Tech</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>24 - 10 - 4</h4>
        <p> 15 tournament appearances /
            Best Finish - Champions (3x) /
            Head Coach - Joe Shawhan
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal1(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Minnesota</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>26 - 9 - 1</h4>
        <p> 40 tournament appearances /
            Best Finish - Champions (5x) /
            Head Coach - Bob Motzko
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal4(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Minnesota State</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>25 - 12 - 1</h4>
        <p> 9 tournament appearances /
            Best Finish - Runner-up /
            Head Coach - Mike Hastings
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal16(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Ohio State</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>20 - 15 - 3</h4>
        <p> 10 tournament appearances /
            Best Finish - Frozen Four (2x) /
            Head Coach - Steve Rohlik
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal11(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Penn State</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>21 - 15 -1</h4>
        <p> 3 tournament appearances /
            Best Finish - DNP /
            Head Coach - Guy Gadowski
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal13(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Quinnipiac</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>30 - 4 - 3</h4>
        <p> 9 tournament appearances /
            Best Finish - Runner-up (2x) /
            Head Coach - Rand Pecknold
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal3(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">St. Cloud State</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>24 - 12 - 3</h4>
        <p> 17 tournament appearances /
            Best Finish - Runner-up /
            Head Coach - Brett Larson
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}

export function VerticallyCenteredModal8(props) {
  return (
    <Modal
      {...props}
      size="lg"
      aria-labelledby="contained-modal-title-vcenter"
      centered
    >
      <Modal.Header closeButton>
        <Modal.Title id="contained-modal-title-vcenter">Western Michigan</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <h4>23 - 14 - 1</h4>
        <p> 8 tournament appearances /
            Best Finish - DNP /
            Head Coach - Pat Ferschweiler
</p>
      </Modal.Body>
      <Modal.Footer>
        <Button onClick={props.onHide}>Close</Button>
      </Modal.Footer>
    </Modal>
  );
}